const bottle = {
    brand: 'Puma',
    price: 450,
    color: 'White',
    isNewArrival: true
}

const keys = Object.keys(bottle); //What are the keys of this object
console.log(keys);
const values = Object.values(bottle); //What are the values of this object
console.log(values);
